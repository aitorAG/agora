# Nginx

Configuración de reverse proxy para exponer servicios:

- Agora API (`engine`) en `:8000`
- Langfuse UI en `:3000`

El archivo `nginx/nginx.conf` es una base local. Ajusta dominios/TLS para producción.
