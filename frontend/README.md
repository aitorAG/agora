# Frontend

UI web estática servida por la API de `engine` cuando `UI_TEST=true`.

- Assets actuales: `frontend/static/`
- Entrada principal: `frontend/static/index.html`

La API monta esta carpeta en `/ui` desde `engine/src/api/app.py`.
